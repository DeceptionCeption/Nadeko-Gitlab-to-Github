using System.Linq;


namespace Nadeko.Common.Services.Db
{
    public static class Extensions
    {
        //public static GuildConfig GuildConfigFor(this CommonContext ctx, ulong guildId)
        //{
        //    var gc = ctx.GuildConfigs.FirstOrDefault(x => x.GuildId == guildId);

        //    if (gc is null)
        //    {
        //        gc = new GuildConfig()
        //        {
        //            GuildId = guildId
        //        };

        //        ctx.GuildConfigs.Add(gc);
        //        ctx.SaveChanges();
        //    }

        //    return gc;
        //}
    }
}
